git clone is a command that is essentially a shortcut for a few other commands.  git clone is used to to recreate a repo on github locally and add a remote as well as pulling the changes made to the repo.  
